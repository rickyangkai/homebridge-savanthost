根据您的反馈，我将进行以下修复：

1.  **修复场景启动失败的问题**：
    *   根据您提供的 `savant_scene_controller.py` 脚本，TypeScript 代码中的场景激活接口 URL 是错误的。
    *   我将把 `src/platform.ts` 中的接口从 `/config/v1/scenes/${sceneId}/activate` 修改为 `/control/v1/scenes/${sceneId}/apply`，以匹配 Python 脚本中的正确逻辑。

2.  **修复激活逻辑并增强检查**：
    *   您提到插件在未激活时就开始寻找主机，这说明激活状态的检查可能被绕过或不够严格。
    *   我将在 `discoverAndSync`、`discoverHost` 和 `fetchScenes` 等核心功能方法中增加强制的 `isActivated` 状态检查，确保只有在真正激活后才会执行这些网络请求。
    *   这将确保如果激活未成功，插件绝对不会进行任何后台扫描或操作。

**实施计划**：

1.  修改 `src/platform.ts`：
    *   更新 `activateScene` 方法的 URL。
    *   在 `discoverAndSync`、`discoverHost`、`fetchScenes` 方法开头添加 `if (!this.isActivated) return;` 防御性代码。

这样可以解决您提到的两个问题。